# ============================================================================
# EMAIL CONFIGURATION - Required for MedMCQ to send emails
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file and rename it to ".env" (remove the .example)
# 2. Fill in your actual email credentials below
# 3. Never commit the .env file to GitHub (it's already in .gitignore)
#
# You need TWO email addresses:
# - noreply@medmcq.com.au (for system emails like verification, password reset)
# - support@medmcq.com.au (for user emails like contact form, reports)
#
# ============================================================================

# SMTP Configuration (for sending emails)
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587

# NoReply Email - Used for automated system emails
NOREPLY_EMAIL=noreply@medmcq.com.au
NOREPLY_PASSWORD=your_noreply_password_here

# Support Email - Used for user-facing communications
SUPPORT_EMAIL=support@medmcq.com.au
SUPPORT_PASSWORD=your_support_password_here

# IMAP Configuration (for reading support inbox in admin panel)
IMAP_HOST=imap.zoho.com
IMAP_PORT=993

# Application URL (your deployed site URL)
APP_URL=https://your-app-url.com

# ============================================================================
# OTHER REQUIRED SETTINGS (likely already configured)
# ============================================================================

# MongoDB Connection
MONGO_URL=your_mongodb_connection_string
DB_NAME=your_database_name

# JWT Secret (for authentication)
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here

# ============================================================================
# PROVIDER-SPECIFIC NOTES
# ============================================================================
#
# If using Gmail (Google Workspace):
# - SMTP_HOST=smtp.gmail.com
# - SMTP_PORT=587
# - IMAP_HOST=imap.gmail.com
# - IMAP_PORT=993
# - Use App Passwords instead of regular passwords
#   (Generate at: https://myaccount.google.com/apppasswords)
#
# If using Zoho (current setup):
# - SMTP_HOST=smtp.zoho.com
# - SMTP_PORT=587
# - IMAP_HOST=imap.zoho.com
# - IMAP_PORT=993
# - Use your regular Zoho password OR app password
#
# ============================================================================
