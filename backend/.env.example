# MedMCQ Backend Environment Variables
# Copy this file to .env and fill in your actual values

# ==============================================================================
# EMAIL CONFIGURATION
# ==============================================================================

# SMTP Configuration (Zoho, Gmail, or other provider)
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587

# NoReply Email - Used for system emails (verification, password reset)
# These emails are sent FROM this address
NOREPLY_EMAIL=noreply@medmcq.com.au
NOREPLY_PASSWORD=your_noreply_password_here

# Support Email - Used for user-facing emails (contact forms, reports, admin emails)
# Both sends FROM and receives TO this address
SUPPORT_EMAIL=support@medmcq.com.au
SUPPORT_PASSWORD=your_support_password_here

# IMAP Configuration - For reading support inbox in admin panel
IMAP_HOST=imap.zoho.com
IMAP_PORT=993

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================

# Application URL (used in email links)
APP_URL=http://localhost:3000

# Database connection
MONGODB_URI=mongodb://localhost:27017/medmcq

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_here_change_in_production

# ==============================================================================
# OPTIONAL: Legacy Email Variables (for backwards compatibility)
# ==============================================================================
# If these are set, they will be used as fallbacks
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM_EMAIL=
# SMTP_FROM_NAME=MedMCQ
# SMTP_REPLY_TO=

# ==============================================================================
# PROVIDER-SPECIFIC NOTES
# ==============================================================================

# For Gmail (Google Workspace):
# - Use App Passwords instead of regular passwords
# - SMTP_HOST=smtp.gmail.com
# - IMAP_HOST=imap.gmail.com
# - Generate App Password at: https://myaccount.google.com/apppasswords

# For Zoho:
# - Use regular email password or generate app-specific password
# - SMTP_HOST=smtp.zoho.com
# - IMAP_HOST=imap.zoho.com
# - Enable IMAP access in Zoho Mail settings

# For AWS SES:
# - Use SMTP credentials from AWS SES console
# - SMTP_HOST=email-smtp.your-region.amazonaws.com
# - No IMAP support (sending only)

# ==============================================================================
# SECURITY REMINDERS
# ==============================================================================
# - Never commit .env files to version control
# - Use strong, unique passwords for email accounts
# - Enable 2FA on email accounts
# - Rotate passwords every 3-6 months
# - Use app-specific passwords when available
